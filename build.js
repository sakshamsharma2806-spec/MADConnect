// MIGRATED: This file previously generated Firebase configuration from environment variables.
// The app now uses Next.js API routes with MongoDB (Mongoose) as the backend.
// Environment variables are stored in .env.local and accessed via process.env.
// The active backend implementation is in lib/mongodb.ts, lib/auth.ts, and app/api/*.
console.log(
  "This build script is no longer needed. The app uses Next.js with MongoDB.",
);
